[project]
name = "fastapi-study"
version = "0.1.0"
description = "主要用于学习fastApi"
authors = [
    {name = "智核",email = "tang.jie@ecar.com"}
]
requires-python = ">=3.13"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
